# 문의 메일 연결

수신처는 서버 코드에서 `sk@samkwang-chem.com`으로 고정한다. 문의자의 주소는 Reply-To로만 사용한다.

Vercel 서버 환경변수에 아래 값을 등록한다. 비밀번호는 채팅·Git·공개 NEXT_PUBLIC 변수에 넣지 않는다.

- SMTP_HOST: 발송 서버
- SMTP_PORT: 465(TLS) 또는 587(STARTTLS 필수)
- SMTP_USER: 발송 계정
- SMTP_PASSWORD: 발송용 비밀번호 또는 앱 비밀번호
- SMTP_FROM: 해당 서버에서 발송 권한이 확인된 이메일 주소

설정 누락은 HTTP503, 입력 오류는400, 타 출처 요청은403, 발송 실패는502로 응답한다. 서버가 수락한 경우에만 성공 화면을 표시한다. 서버 수락은 최종 받은편지함 도착을 보장하지 않으므로 실제 수신함 검수가 필요하다.

개인정보는 본문에 포함되어 담당자 메일함으로 전달되며 별도 DB에는 저장하지 않는다. 개인정보 처리방침·보유기간의 운영 확정, 배포 환경의 지속성 있는 rate limit/CAPTCHA 도입은 공개 운영 전 필수다. 현재는 입력 제한·동일 출처 검사·honeypot만 구현되어 있다.

SMTP 공식 참고: https://nodemailer.com/smtp

현재 실제 SMTP 자격증명 미연결. 실메일 발송·수신 검증 미실시.

검수: Next 프로덕션 빌드 및 tsc 통과. 실제 브라우저에서 빈 필수 입력 차단, 제품명 자동 입력, 정상 형식 제출 시 SMTP 미연결 오류 표시 및 입력 유지 확인. 375px 모바일 양식·버튼 표시 확인. 잘못된 API 본문은 HTTP400 확인. 실제 발송 성공 경로는 자격증명 연결 전이므로 미검증.
